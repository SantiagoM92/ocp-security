{{- if .Values.clusterRoleBinding.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.clusterRoleBinding.name }}
  namespace: {{ .Values.serviceAccount.namespace | default .Release.Namespace }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.serviceAccount.name | default (include "ocp-security.name" .) }}
    namespace: {{ .Values.serviceAccount.namespace | default .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: {{ .Values.clusterRoleBinding.clusterRoleName }}
  apiGroup: {{ .Values.clusterRoleBinding.apiGroup | default "rbac.authorization.k8s.io" }}
{{- end }}
